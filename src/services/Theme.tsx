import { defaultTheme } from 'evergreen-ui'

/**
 * Returns the standard Evergreen UI theme.
 *
 * We may want to over-ride defaults later, so we import
 * the theme from a central place.
 */
export const theme = defaultTheme
