/**
 * The default options used to debug regexes hashes,
 * if no user-defined options were provided.
 */
export const defaultOptions = {
  modifiers: [],
}

/**
 * A string which uniquely identifies this output function.
 */
export const id = 'regex'
